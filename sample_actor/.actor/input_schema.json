{
  "title": "Sample Actor input",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "greeting": {
      "title": "Greeting",
      "type": "string",
      "description": "Text the Actor echoes back into its OUTPUT record.",
      "editor": "textfield",
      "default": "hello"
    },
    "tone": {
      "sectionCaption": "Message options",
      "sectionDescription": "Fine-tune how the greeting is generated.",
      "title": "Tone",
      "type": "string",
      "description": "Style of the greeting message.",
      "editor": "select",
      "enum": ["friendly", "formal", "playful"],
      "enumTitles": ["Friendly", "Formal", "Playful"],
      "default": "friendly"
    },
    "repeatCount": {
      "title": "Repeat count",
      "type": "integer",
      "description": "How many times to repeat the greeting.",
      "default": 1,
      "minimum": 1
    },
    "shout": {
      "title": "Shout",
      "type": "boolean",
      "description": "Uppercase the greeting before writing it out.",
      "default": false
    },
    "recipients": {
      "sectionCaption": "Delivery",
      "sectionDescription": "Who the greeting is addressed to.",
      "title": "Recipients",
      "type": "array",
      "description": "Names to greet individually.",
      "editor": "stringList",
      "prefill": ["Ada", "Grace"]
    },
    "advanced": {
      "sectionCaption": "Advanced",
      "sectionDescription": "Free-form JSON passed through to the Actor unchanged.",
      "title": "Advanced options",
      "type": "object",
      "description": "Free-form JSON passed through untouched (no dedicated widget).",
      "editor": "json",
      "default": {}
    }
  },
  "required": ["greeting"]
}
