{
	"$schema": "https://apify.com/schemas/v1/actor.ide.json",
	"actorSpecification": 1,
	"name": "my-resources-actor",
	"title": "TypeScript resource-reporting sample actor for actor-runtime",
	"description": "Prints the memory and CPU granted to the run (SDK environment) and the usage reported by each systemInfo event (SDK event manager).",
	"version": "0.0",
	"buildTag": "latest",
	"meta": {
		"templateId": "ts-empty"
	},
	"dockerfile": "../Dockerfile"
}
